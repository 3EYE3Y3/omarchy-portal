[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "omarchy-portal"
version = "0.9.1"
description = "Secure phone-to-Omarchy control plane"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "3EYE3Y3" }]
dependencies = []

[project.scripts]
portal = "portal.cli:main"

[tool.setuptools]
packages = ["portal"]
